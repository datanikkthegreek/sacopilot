bundle:
  name: sacopilot

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    sacopilot-app:
      name: "sacopilot"
      description: "sacopilot created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
